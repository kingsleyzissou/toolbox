arch: "aarch64"

images:
  - location: "~/scratch/VMs/fedora-43.aarch64.qcow2"
    arch: "aarch64"

mountType: virtiofs

mounts:
  - location: "~/Projects"
    writable: true
  - location: "~/scratch/output"
    mountPoint: "/output"
    writable: true
  - location: "~/Projects/play/toolbox/blueprints/"
    mountPoint: "/config"
    writable: false

portForwards:
  - guestPort: 9090
    hostPort: 9090

containerd:
  system: false
  user: false

env:
  # this is just a small convenience to ignore some zsh configs
  CONTAINER_ID: "lima"
  ZDOTDIR: "/home/zissou.linux/.config/zsh"

provision:
  - mode: system
    script: usermod -aG wheel zissou

  - mode: system
    # this was causing loads of issues
    script: setenforce 0

  - mode: user
    script: |
      git clone https://github.com/kingsleyzissou/.dotfiles.git
      cd .dotfiles && git checkout containers && stow . && rm ~/.zshrc
